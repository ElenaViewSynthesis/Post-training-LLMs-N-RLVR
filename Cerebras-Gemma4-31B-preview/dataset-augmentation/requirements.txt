# Core pipeline
dask[dataframe]>=2024.1.0
pyarrow>=15.0.0
pandas>=2.0.0
fsspec>=2024.1.0

# HuggingFace — streaming (hf:// protocol), fast CDN upload, dataset push
huggingface_hub>=0.23.0
hf_transfer>=0.1.6
datasets>=2.19.0

# Cerebras Inference (teacher model — fallback when SageMaker endpoint not set)
cerebras-cloud-sdk>=1.0.0

# Gemini Developer API (resume worker after Cerebras quota exhaustion)
google-genai[aiohttp]>=1.33.0

# SageMaker deployment + inference (deploy_sagemaker.py + gemma4_31b_agent.py)
sagemaker<3.0.0
boto3>=1.34.0

# Together.ai + OpenAI-compatible backends
openai>=1.30.0

# Utilities
python-dotenv>=1.2.2
tqdm>=4.66.0
